[project]
name = "text-rag-cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "itznrd", email = "natarajdas007@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "chromadb>=1.5.9",
    "dotenv>=0.9.9",
    "groq>=1.7.0",
]

[project.scripts]
text-rag-cli = "text_rag_cli:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
package = false
